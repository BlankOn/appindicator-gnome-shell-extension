# Our packaging lives on 'sinambung', rebased on top of upstream's master.
# Nothing outside debian/ is modified, so `git rebase upstream/master`
# should never conflict.
[DEFAULT]
debian-branch = sinambung
upstream-branch = master
